---
"cron-explain-ja": minor
---

`parse` が「休日」を曜日として読めるようにした

祝日や振替休日は cron 式に書けないので、「休日」は週末（`0,6`）に寄せて解釈する。
黙って寄せると祝日ぶんの取りこぼしが見えないため、`notes` に「祝日には実行されません」と
理由を残し、`confidence` を 0.2 下げる（「休日の10時」→ `0 1 * * 0,6`、confidence 0.8）。
曜日そのものは決まるので `ambiguities` には出さない。「週末」「土日」の扱いは今までどおり。
